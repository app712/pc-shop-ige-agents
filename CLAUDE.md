# PC SHOP IGE 開発・運用 総合統合ガイドライン(抜粋)

制定日: 2026年8月16日 / 対象: 全開発担当・全AI社員
正本はGoogleドライブ上の共有ドキュメント。本ファイルはクラウド定期実行(ルーティン)向けの抜粋転記。

## 1. 開発の基本理念
PC SHOP IGEは、手動作業を極限まで排除し、GitHubとCloudflare Pagesを用いた、GitHub連携による
全自動デプロイ(CI/CD)を標準開発環境とする。

## 2. 技術スタック定義
- フロントエンド: HTML / CSS / Vanilla JavaScript
- バージョン管理: Git / GitHub(アトミック構成)
- ホスティング: Cloudflare Pages
- バックエンド: Google Apps Script (GAS)
- DB/データストア: Googleスプレッドシート / Sheets API (v4)

## 3. 開発・運用ワークフロー
1. ブランチ作成: 開発は必ず `feature-xxx` ブランチで実施。`main` への直接プッシュは厳禁。
2. プルリクエスト(PR): 実装完了後、GitHub上でPRを発行。
3. 自動レビュー: Cloudflare Pagesのプレビュー用レビューURLで動作検証。
4. 承認・デプロイ: QA通過後、承認(Approve)を経て `main` へマージ、自動的に本番反映。

## 4. 厳定・絶対ルール
- **ローカル保存の禁止**: コード・データをローカルのみに置かず、GitHubとクラウド(Drive/Sheets)で
  即座に同期・一元管理すること。
- **ハードコーディングの禁止**: 環境変数(APIキー・シートID等)はコードに直接書かず、
  プロパティサービスまたは設定シートで管理する。
- **通信の堅牢化**: 全API通信で try-catch を実装し、エラー時は `ContentService.MimeType.JSON` で
  レスポンスを返すこと。
- **手戻り・齟齬の絶無**: AI社員間のコミュニケーションは、感情的な表現や不要な文脈を排し、
  技術要件と結論のみを即座に提示すること。

## 5. AI社員ロール別規範
- フロントエンジニア: フロントエンド独立性の構築。WordPress等CMSに依存しないコード管理。
- GASエンジニア(`gas-engineer`): バックエンドAPI構築と6分制限を考慮したバッチ・非同期処理設計。
- QAエンジニア(`qa-engineer`): エッジケーステストの実施とデバッグコンソールの可視化。
- インフラエンジニア(`security-infra-engineer`): GitHub権限・ブランチ保護ルールの監視。
- マーケター(`marketer`) / LP作成担当(`lp-creator`): 機能ではなく便益(Benefits)を重視した訴求設計。

このリポジトリの `.claude/agents/` には上記ロールを含む11体のAI社員(サブエージェント)定義がある。
週次の定期調査ルーティンは、これらをAgentツールで呼び出して各領域の最新情報を収集し、
`reports/` 配下にレポートとしてまとめてコミットする。
